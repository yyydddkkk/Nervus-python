# 安全说明

## 支持范围

当前维护版本为 `0.1.0a1` 开发者预览版，仅验证 Linux / CPython 3.14.x。没有长期支持或安全响应时限承诺。

## 执行边界

Nervus 执行可信 Python 代码，**不是沙箱**。Worker、能力和外部程序可访问文件、网络及其他宿主资源。应用方必须自行提供隔离、最小权限和凭证管理，不要直接执行不可信代码。

受管进程的 POSIX 进程组不覆盖主动脱离的程序，也不保证监督端崩溃后的回收。更多使用限制见 [README](README.md)。

## 报告漏洞

不要在公开 issue、PR 或讨论中提交密钥、个人信息、敏感日志或可利用细节。

若仓库提供 GitHub 私密漏洞报告功能，请使用该功能。否则仅提交不含敏感内容的问题，请维护者提供私密渠道；获得渠道前保留详细材料。

私密报告请包含版本、运行环境、影响范围和脱敏的最小复现。不要为报告访问他人数据或系统。泄露的密钥应立即撤销或轮换；仅删除公开文本不能恢复密钥安全。
